# Routes
# This file defines all application routes (Higher priority routes first)
# ~~~~


POST     /user/register         pl.pgizka.gsenger.core.UserController.loginFacebookUser
GET      /user/getFriends       pl.pgizka.gsenger.core.UserController.getFriends

PUT      /chat/create           pl.pgizka.gsenger.core.ChatsController.createChat
GET      /chat/listAll          pl.pgizka.gsenger.core.ChatsController.listAllChatsWithParticipantInfo

PUT      /message/create        pl.pgizka.gsenger.core.MessageController.createMessage

# Map static resources from the /public folder to the /assets URL path
GET     /assets/*file           controllers.Assets.at(path="/public", file)

    